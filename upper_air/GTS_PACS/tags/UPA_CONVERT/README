This directory contains the files that are needed to process the
GTS data that JOSS receives from SCD.

The *FORTRAN* file contains a directory of Scot's that contains
fortran code that converts the data received from SCD ASCII into
another ASCII format that is used as the input for the conversion
to class files.